# STP - отказоустойчивость повышается но требуется 15-30 секунд пока перенастроится. RSTP - STP c ускоренной реконфигурацией дерева.
# Spanning Tree Protocol(STP) - канальный протокол (второй уровень модели OSI) - Основной задачей STP является устранение петель в топологии произвольной сети 
# Ethernet, в которой есть один или более сетевых мостов, связанных избыточными соединениями. 
# STP решает эту задачу, автоматически блокируя соединения, которые в данный момент для полной связности коммутаторов являются избыточными.

# Берем в пример 3 коммутатора которые соеденины между собой, при этом один из портов будет автоматически заблокирован. в случае обрыва используещегося соединения
# включится заблокированный порт и соединение востановится
# проверим корневой коммутатор
en (привилегированный режим)
show spanning-tree (посмотрим кто назначет корневым коммутатором и подключения на комутаторе)
   This bridge is the root(надпись свидетельствует что комутатор корневой)
   
# Для проверки работы резервного соединения можем убить подключение работающих портов
configure t(режим конфигурации)
interface fa0/2(вибираем нужный порт)
shutdown (отключаем порт)
# После этого включается автоматически заблокированный порт

# включить выключенный порт
configure t(режим конфигурации)
interface fa0/2(вибираем нужный порт)
no shutdown (включаем порт)

# Включение RSTP на коммутаторах Cisco этот протокол называется pvst
configure t(режим конфигурации)
spanning-tree mode rapid-pvst (включение протокола)

# просмотр 
en (привилегированный режим)
show spanning-tree (тут смотрим на строку ниже)
   Spanning tree enabled protocol rstp (включен протокол rspt)
